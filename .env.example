# Copy this file to .env.local and set variables as needed.
#
# API route is fixed to local backend via Next.js rewrite:
# /api/* -> http://127.0.0.1:8000/*
# No environment variables are required for backend routing.
